defmodule LangChain.Chains.Utils.ChainValidator do
  @moduledoc """
  Validation utilities for chain configurations and inputs.
  """

  def validate_config(config) do
    {:ok, config}
  end

  def validate_input(input) do
    {:ok, input}
  end

  def validate_output(output) do
    {:ok, output}
  end
end
