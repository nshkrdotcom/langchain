defmodule LangChain.Integration.Capabilities.ChatFlowTest do
  use LangChain.BaseTestCase

  @moduletag :integration

  describe "complete chat flow" do
    test "successfully completes a chat interaction with middleware" do
      # Test implementation
    end
  end
end
